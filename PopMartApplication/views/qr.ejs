<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Order QR</title>
  <style>
    body { margin:0; display:flex; justify-content:center; align-items:center; min-height:100vh; background:#f8fafc; font-family: "IBM Plex Sans", "Segoe UI", Arial, sans-serif; }
    .card { background:#fff; padding:22px; border-radius:18px; border:1px solid #e2e8f0; box-shadow: 0 4px 16px rgba(0,0,0,0.04); text-align:center; }
    .muted { color:#475569; font-size:14px; margin:8px 0 0; }
  </style>
</head>
<body>
  <div class="card">
    <a id="qrLink" href="<%- trackUrl %>" target="_blank" rel="noopener" style="display:inline-block;">
      <div id="qr" style="width:320px; height:320px; margin:0 auto;"></div>
    </a>
    <p class="muted">
      Scan or tap to view order status for #<%= order.id %>. QR refreshes in
      <span id="countdown">03:00</span>.
    </p>
  </div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
  <script>
    (function () {
      var url = "<%- trackUrl %>";
      var qrEl = document.getElementById("qr");
      new QRCode(qrEl, { text: url, width: 320, height: 320, correctLevel: QRCode.CorrectLevel.M });
      var qrLink = document.getElementById("qrLink");
      qrLink.href = url;

      // 3-minute countdown then reload to fetch a fresh token/QR.
      var ttlMs = 180000;
      var countdownEl = document.getElementById("countdown");
      var end = Date.now() + ttlMs;

      function format(ms) {
        var total = Math.max(0, Math.floor(ms / 1000));
        var m = String(Math.floor(total / 60)).padStart(2, "0");
        var s = String(total % 60).padStart(2, "0");
        return m + ":" + s;
      }

      function tick() {
        var remaining = end - Date.now();
        countdownEl.textContent = format(remaining);
        if (remaining <= 0) {
          window.location.reload();
        } else {
          requestAnimationFrame(tick);
        }
      }
      tick();
    })();
  </script>
</body>
</html>
