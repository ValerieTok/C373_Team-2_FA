<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <style>
    @import url("https://fonts.googleapis.com/css2?family=Spectral:wght@600;700&family=IBM+Plex+Sans:wght@400;500;600&display=swap");
    :root {
      --ink: #0f172a;
      --ink-soft: #475569;
      --paper: #f7f4ef;
      --card: #ffffff;
      --line: #e5e7eb;
      --accent: #111827;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "IBM Plex Sans", "Segoe UI", Arial, sans-serif;
      color: var(--ink);
      background: #f7f4ef;
      min-height: 100vh;
    }
    .nav {
      position: sticky;
      top: 0;
      z-index: 10;
      background: #f7f4ef;
      border-bottom: 1px solid var(--line);
    }
    .nav-inner {
      max-width: 1120px;
      margin: 0 auto;
      padding: 16px 18px 10px;
      display: flex;
      gap: 18px;
      align-items: center;
      flex-wrap: wrap;
    }
    .brand {
      font-family: "Spectral", "Times New Roman", serif;
      font-size: 18px;
      letter-spacing: 1px;
      padding: 6px 14px;
      border-radius: 12px;
      background: #111827;
      color: #f9fafb;
    }
    .nav-links {
      display: flex;
      gap: 18px;
      flex-wrap: wrap;
      align-items: center;
      border-top: 1px solid var(--line);
      width: 100%;
      padding-top: 10px;
    }
    .nav-links a {
      text-decoration: none;
      color: var(--ink);
      font-weight: 600;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .nav-links a:hover {
      color: #0f172a;
    }
    .cart-pill {
      padding: 2px 8px;
      background: #111827;
      color: #f9fafb;
      border-radius: 999px;
      font-size: 10px;
      letter-spacing: 0.6px;
      margin-left: 4px;
    }
    .wallet {
      margin-left: auto;
      display: flex;
      gap: 10px;
      align-items: center;
      flex-wrap: wrap;
    }
    .container {
      max-width: 1120px;
      margin: 28px auto 60px;
      padding: 0 18px;
    }
    .page-card {
      background: var(--card);
      border-radius: 18px;
      border: 1px solid var(--line);
      padding: 22px;
    }
    .card {
      background: var(--card);
      border-radius: 16px;
      border: 1px solid var(--line);
      padding: 16px;
      box-shadow: none;
    }
    .btn {
      border: 1px solid #111827;
      background: #111827;
      color: #f9fafb;
      padding: 8px 16px;
      border-radius: 999px;
      font-weight: 600;
      letter-spacing: 0.6px;
      text-transform: uppercase;
      font-size: 12px;
      cursor: pointer;
    }
    .btn-outline {
      background: transparent;
      color: #111827;
    }
    .btn-ghost {
      background: transparent;
      border: 1px solid #111827;
      color: #111827;
    }
    input, textarea {
      font-family: inherit;
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 10px 12px;
      background: #ffffff;
    }
    input:focus, textarea:focus {
      outline: none;
      border-color: #111827;
      box-shadow: 0 0 0 2px rgba(15, 23, 42, 0.12);
    }
    .pill {
      background: #f1f5f9;
      color: #334155;
      border-radius: 999px;
      padding: 4px 10px;
      font-size: 11px;
      letter-spacing: 0.4px;
      text-transform: none;
    }
    .role-tag {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 4px 10px;
      border-radius: 999px;
      background: #111827;
      color: #f9fafb;
      font-size: 11px;
      letter-spacing: 0.6px;
      text-transform: uppercase;
    }
    .muted { color: var(--ink-soft); }
    .mono { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace; }
    .list {
      display: flex;
      flex-direction: column;
      gap: 14px;
    }
    .listing-row {
      display: grid;
      grid-template-columns: 180px 1fr;
      gap: 18px;
      align-items: center;
      border: 1px solid var(--line);
      border-radius: 16px;
      padding: 14px;
      background: #ffffff;
    }
    .listing-image {
      background: #f3f4f6;
      border-radius: 12px;
      padding: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .listing-image img {
      width: 100%;
      height: 140px;
      object-fit: contain;
      border-radius: 10px;
    }
    .listing-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 12px;
    }
    .listing-title {
      margin: 0;
      font-family: "Spectral", "Times New Roman", serif;
      font-size: 20px;
    }
    .listing-desc { margin: 8px 0 12px; }
    .grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 18px;
    }
    .list-item {
      display: grid;
      grid-template-columns: 110px 1fr;
      gap: 14px;
      align-items: center;
    }
    .thumb {
      width: 100%;
      height: 90px;
      border-radius: 12px;
      border: 1px solid var(--line);
      background: #f5f0e6;
      object-fit: cover;
    }
    .stack {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .badge {
      display: inline-flex;
      gap: 6px;
      align-items: center;
      font-size: 12px;
      padding: 4px 10px;
      border-radius: 999px;
      background: #f1f5f9;
      color: #475569;
    }
    .timeline {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin: 8px 0 0;
      padding-left: 14px;
      border-left: 2px solid #e5e7eb;
    }
    .timeline-item {
      position: relative;
      padding-left: 10px;
      font-size: 12px;
      color: #475569;
    }
    .timeline-item::before {
      content: "";
      position: absolute;
      left: -18px;
      top: 4px;
      width: 8px;
      height: 8px;
      background: #111827;
      border-radius: 999px;
    }
    @media (max-width: 900px) {
      .listing-row { grid-template-columns: 1fr; }
      .grid { grid-template-columns: 1fr; }
      .nav-links { gap: 12px; }
    }
  </style>
</head>
<body>
  <nav class="nav">
    <div class="nav-inner">
      <div class="brand">Labubu Chain</div>
      <div class="wallet">
        <button class="btn btn-ghost" id="connectWalletBtn" type="button">Connect wallet</button>
        <button class="btn btn-outline" id="disconnectWalletBtn" type="button">Disconnect</button>
        <span class="mono" id="walletAddress" style="font-size:12px;"></span>
      </div>
      <div class="nav-links">
        <a href="/">Listings</a>
        <a href="/seller">Seller</a>
        <a href="/buyer">Buyer</a>
        <a href="/cart">Cart <span class="cart-pill"><%= cartCount %></span></a>
      </div>
    </div>
  </nav>
  <div class="container">
